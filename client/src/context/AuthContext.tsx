import { createContext,useContext,useState,type ReactNode } from 'react';import type { User } from '../types';import { api } from '../services/api';
type Ctx={user:User|null;login:(e:string,p:string)=>Promise<void>;register:(n:string,e:string,p:string)=>Promise<void>;logout:()=>void};const AuthContext=createContext<Ctx|null>(null);
export function AuthProvider({children}:{children:ReactNode}){const [user,setUser]=useState<User|null>(()=>{const raw=localStorage.getItem('user');return raw?JSON.parse(raw):null});async function save(path:string,body:object){const d=await api<{user:User;token:string}>(path,{method:'POST',body:JSON.stringify(body)});localStorage.setItem('token',d.token);localStorage.setItem('user',JSON.stringify(d.user));setUser(d.user)}return <AuthContext.Provider value={{user,login:(email,password)=>save('/auth/login',{email,password}),register:(name,email,password)=>save('/auth/register',{name,email,password}),logout:()=>{localStorage.clear();setUser(null)}}}>{children}</AuthContext.Provider>}
export const useAuth=()=>{const c=useContext(AuthContext);if(!c)throw new Error('AuthProvider missing');return c};

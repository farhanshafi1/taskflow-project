import { useState,type FormEvent } from 'react';import { useNavigate,Link } from 'react-router-dom';import { useAuth } from '../context/AuthContext';export function Register(){const [f,setF]=useState({name:'',email:'',password:''});const [error,setError]=useState('');const auth=useAuth();const nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();if(f.name.trim().length<2)return setError('Name must contain at least 2 characters');if(f.password.length<8)return setError('Password must contain at least 8 characters');try{await auth.register(f.name,f.email,f.password);nav('/dashboard')}catch(e){setError((e as Error).message)}}return <section className="card auth"><h1>Create account</h1><form onSubmit={submit}><label>Name<input value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email<input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Password<input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label>{error&&<p role="alert" className="error">{error}</p>}<button>Create account</button></form><p>Already registered? <Link to="/login">Sign in</Link></p></section>};

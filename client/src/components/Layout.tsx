import { Link,Outlet } from 'react-router-dom';import { useAuth } from '../context/AuthContext';export function Layout(){const {user,logout}=useAuth();return <><header><nav aria-label="Main navigation"><Link className="brand" to="/">TaskFlow</Link>{user&&<><Link to="/dashboard">Dashboard</Link><button onClick={logout}>Logout</button></>}</nav></header><main id="main" className="container"><Outlet/></main><footer>TaskFlow · Accessible project management</footer></>};
